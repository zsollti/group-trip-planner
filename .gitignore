# ---- Dependencies ----
node_modules/
.pnp/
.pnp.*

# ---- Build output ----
dist/
dist-test/
build/
out/
*.tsbuildinfo
.turbo/

# ---- Test / coverage ----
coverage/
.nyc_output/

# ---- Playwright (Phase 7.4) ----
# Reports and traces are per-run artefacts; CI uploads them on failure.
playwright-report/
test-results/
.playwright/

# ---- Environment & secrets ----
.env
.env.*
!.env.example

# ---- Logs ----
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# ---- OS / editor cruft ----
.DS_Store
Thumbs.db
.idea/
*.swp

# ---- Claude Code local settings ----
.claude/settings.local.json

# ---- Uploaded images (local-disk storage driver, Phase 6.1) ----
# User content, never source. The R2 driver replaces this directory entirely.
var/
apps/api/var/

# ---- Local planning docs (kept local, not tracked) ----
SRS.md
PLANNING.md
IMPLEMENTATION.md
UI-SPECS.md

# ---- Personal CV/LinkedIn workspace (never published) ----
# The repository is public. Everything in here is private job-search material:
# CV drafts, the rendered PDF, and the LinkedIn/portfolio notes.
job-search/

# Kept so the old repo-root copies stay ignored if one is ever restored.
linkedin.md
aurora.md
cv.html
cv.pdf
