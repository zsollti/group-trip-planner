/**
 * @gtp/ui-primitives — shared low-level primitives ONLY (buttons, inputs,
 * modals, form controls). Layout, navigation, and interaction stay per-app
 * (§0b). Styled per app via data-attributes + each app's design tokens.
 */
export { Button } from "./Button.js";
export type { ButtonProps, ButtonVariant } from "./Button.js";
